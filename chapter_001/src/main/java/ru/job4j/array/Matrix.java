package ru.job4j.array;

/**
 * Matrix
 *
 * @author Alexander Kordyukov (alex-programm@yandex.ru)
 * @version $Id$
 * @since 0.1
 */
public class Matrix {
    /**
     * The method fills fields of a matrix with multiplication table elements
     * according to dimension of this matrix
     * @param size
     * @return the matrix filled with multiplication table elements
     */
    public int[][] multiple(int size) {
        int[][] table = new int[size][size];
        for (int i = 0; i < size; i++) {
            for (int j = 0; j < size; j++) {
                table[i][j] = (i + 1) * (j + 1);
            }
        }
        return table;
    }
}